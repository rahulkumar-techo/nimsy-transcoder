<!-- 
const base = `production-video/${video.id}`;

const urls = {
  "720p": `${base}/720p/video.mp4`,
  "480p": `${base}/480p/video.mp4`,
  "360p": `${base}/360p/video.mp4`,
  "240p": `${base}/240p/video.mp4`,
  "144p": `${base}/144p/video.mp4`,
};
 -->